<%= form_for(task_word, url: task_task_words_path) do |f| %>
  <% if task_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_word.errors.count, "error") %> prohibited this task_word from being saved:</h2>

      <ul>
      <% task_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %>
    <%= f.text_field :task_id %>
  </div>

  <div class="field">
    <%= f.label :word_id %>
    <%= f.text_field :word_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
