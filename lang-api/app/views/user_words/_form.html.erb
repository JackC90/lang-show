<%= form_for(user_word) do |f| %>
  <% if user_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_word.errors.count, "error") %> prohibited this user_word from being saved:</h2>

      <ul>
      <% user_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :word_id %>
    <%= f.text_field :word_id %>
  </div>

  <div class="field">
    <%= f.label :is_owner %>
    <%= f.check_box :is_owner %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
